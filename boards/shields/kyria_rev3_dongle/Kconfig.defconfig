if SHIELD_KYRIA_REV3_DONGLE

config ZMK_KEYBOARD_NAME
    default "Kyria"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

config ZMK_SPLIT
    default y

# Kyria has two halves
config ZMK_SPLIT_BLE_CENTRAL_PERIPHERALS
    default 2

# ZMK_SPLIT_BLE_CENTRAL_PERIPHERALS + desired BT profiles (default 5)
config BT_MAX_CONN
    default 7

config BT_MAX_PAIRED
    default 7

endif
